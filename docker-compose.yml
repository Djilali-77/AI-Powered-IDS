version: '3.8'

services:
  backend:
    build: ./back-end
    container_name: ids-backend
    ports:
      - "8000:8000"
    volumes:
      - ./back-end:/app

  frontend:
    build: ./front-end
    container_name: ids-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend